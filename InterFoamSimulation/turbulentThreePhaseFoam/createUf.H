// createUf.H — thin wrapper kept for readability; the real work (creating the
// face-flux velocity field Uf if the case's 0/Uf exists) is in the stock
// OpenFOAM header below.
#include "createUfIfPresent.H"
